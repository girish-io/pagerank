package io.girish;

import io.girish.core.CommandParser;

public class Main {
    public static void main(String[] args) {
        CommandParser commandParser = new CommandParser();
        commandParser.run(args);

        // TODO: Generate random graphs of size <n> with random relationships between nodes,
        //  with <n> being the number of nodes inside the graph
    }
}
